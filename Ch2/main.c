#include <stdio.h>

int main()
{
    int a = 5;
    int b = 10;
    
    /*
        여기서는 두 변수 a,b의 합을 계산하고
        결과를 화면에 출력합니다.
        이 부분은 여러 줄 주석으로 처리되었습니다.
    */
    
    //아래 코드는 현재 비활성화되어 있습니다.
    //printf("a + b = %d\n", (a+b));

    return 0;
}
